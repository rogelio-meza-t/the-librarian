<div class="nested-fields">
  <%= f.select :id, options_for_select(Author.all.map{|s|[s.name, s.id]}, (f.object) ? f.object.id : 1),class: "form-control" %>
  <%= f.hidden_field :_new, :value => (!f.object.id.nil?) ? "1" : "false" %>
  <%= link_to_remove_association "Eliminar author", f%>
</div>
